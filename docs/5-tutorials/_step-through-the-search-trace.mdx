import stepThroughTrace from './step-through-trace.png';


- In the **Steps** panel, choose the search trace layer from the layer dropdown. Here, the layer is named **{props.traceName ?? "Cross Example"}**.
- Click **Play**.

<img src={props.image ?? stepThroughTrace} alt="Step through trace" />


